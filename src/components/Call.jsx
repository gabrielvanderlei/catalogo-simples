import React from 'react';
import './Call.css';

export default ({name, price, onSelect}) => (
    <div class="call">
        <h1>Pedir agora</h1>
        <a className="whatsapp-btn" href="#">Whatsapp: </a>
    </div>
)